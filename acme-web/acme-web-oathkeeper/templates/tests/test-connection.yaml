apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acme-web-oathkeeper.fullname" . }}-test-connection"
  labels:
    {{- include "acme-web-oathkeeper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acme-web-oathkeeper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
