package com.acme.web.test.specs

// import io.mockk.mockk

class PrescribeProductSpec : Spec() {

  // private val appointmentRepository = mockk<AggregateRepository<Appointment, Appointment.Id>>(relaxUnitFun = true)
  // private val patientRepository = mockk<AggregateRepository<Client, ClientId>>(relaxUnitFun = true)
  // private val practiceRepository = mockk<AggregateRepository<Practice, PracticeId>>(relaxUnitFun = true)
  // private val practitionerRepository = mockk<AggregateRepository<Practitioner, PractitionerId>>(relaxUnitFun = true)

  // @TestTemplate
  // fun `should prescribe product`(driver: WebDriver) {
  // Given
  // val api = DefaultAppointmentsAPI(
  //   AggregatePersistenceModule(
  //     appointmentRepository,
  //     patientRepository,
  //     practiceRepository,
  //     practitionerRepository
  //   )
  // )
  //
  // api.exec(CreatePracticeCommand(PractitionerId(),"Acme Doctors")).also { practiceId ->
  //   api.exec(CreatePractitionerCommand(UserId(), HumanName("Johnson", "Debbie"), Gender.UNKNOWN)).also { practicionerId ->
  //     api.exec(CreateClientCommand(HumanName("Smith", "Mary"), Gender.UNKNOWN)).also { patientId ->
  //
  //     }
  //   }
  // }
  // }
}
