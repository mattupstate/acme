{
  "name": "acme-handbook",
  "version": "0.1.0",
  "description": "Acme project documentation",
  "scripts": {
    "buildAntoraSite": "antora --fetch antora-playbook.yml",
    "publishAntoraSite": "rm -rf build/site && antora --fetch antora-playbook.yml && touch build/site/.nojekyll && gh-pages-clean && gh-pages --dotfiles --dist build/site --no-history"
  },
  "devDependencies": {
    "@antora/cli": "3.0.0",
    "@antora/site-generator": "3.0.0",
    "asciidoctor-kroki": "^0.15.4",
    "asciidoctor-plantuml": "^1.5.0",
    "gh-pages": "^4.0.0"
  },
  "dependencies": {
    "@antora/lunr-extension": "^1.0.0-alpha.8"
  }
}
