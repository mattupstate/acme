apiVersion: v1
kind: Service
metadata:
  name: {{ include "acme-app-web.fullname" . }}
  labels:
    {{- include "acme-app-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acme-app-web.selectorLabels" . | nindent 4 }}
