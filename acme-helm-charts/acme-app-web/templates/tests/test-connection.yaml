apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acme-app-web.fullname" . }}-test-connection"
  labels:
    {{- include "acme-app-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acme-app-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
